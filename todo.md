# 待办事项

2026-08-18 内容审查的遗留项，以及 2026-09-23 修复后仍待决策的部分，供维护者参考。

## 1. 事务所（经纪公司）tag 命名整体策略 ✅ 已处理

原先 tag 英日混用（英文 `T-POWERS`/`8MAN`/`LIGHT`，日文 `プライムエージェンシー`/`オールプロモーション` 等），且存在 `前X` 变体与同一公司的写法分叉。

2026-09-23 已按以下口径三语统一，规则全文见 `TRANSLATION.md` 的「事务所 tag 命名规范」：

- 同一家公司只用一个 tag；
- 有经纪公司条目页的用条目名（`Allpro`、`Prime Agency`、`8MAN`、`Diaz Group`…）；
- 无条目页的用官方原文（`カプセルエージェンシー`、`リスタープロ`、`名東`…）；
- 曾所属写进正文的 `经纪公司` 字段，不造 `前X` tag；
- 改名用现名（`8MAN` 而非 `SO MODEL AGENT`、`Allpro` 而非 `オールプロ`）。

**遗留**：刻意未做整体日化/英化——那会破坏官方写法与条目名的绑定。若日后要统一，需一次性重命名 tag 并同步所有条目页表格。

## 2. 女优基本信息格式不统一 ✅ 已处理

- 多数页面用 `- **姓名**: ...` 列表式
- 原先唯一表格式的 `鹰宮ゆい.md`（`| 项目 | 内容 |`）已于 2026-09-23 改为列表式（三语同步），字段保留（艺名/中文译名/别名/出生/血型/事务所/作品数等）
- 男の娘等条目用「艺名」字段（如 `おりん.md`）作为例外保留

## 3. 正文与 tag 拼写不一致 ✅ 已处理

审查举例的 4 项已修复：`吉根ゆりあ`（ビーダッシュプロモーション → B-dash）、`あきばもも`（アルシェ → ARCHE-Production）、`あずみ恋`、`水卜樱`。`美園和花` 正文已是 `B-dash`，无需改动。

新增的 tag 改名也已回写正文，统一为「`条目名（官方原文）`」并列形式，涉及仁科百华、樱木凛、莲实克蕾儿、羽田爱、篠田步美、佐佐木明希、北川绘里香（三语同步）。

**遗留**：部分页面正文不提事务所（只靠 tag 携带该信息），如 `市川雅美`、`大槻响`、`五日市芽依`。属信息缺失而非写法冲突，未改。

## 4. 经纪公司条目缺失 ✅ 主体已处理

2026-09-23 新建 `LIGHT.md`、`Prime Agency.md`、`Allpro.md` 三个条目页（三语），并把 `经纪公司/index.md` 的标题挂上链接、表格补 3 行，同时修正过期的「知名女优」举例：

- 石川澪已不在 Allpro，改为北川绘里香 / 羽田爱 / 篠田步美；
- 永井マリア已转 Magiito，Prime Agency 现役改为仁科百华 / 樱木凛。

**已补齐（2026-09-23）**：

- `经纪公司/Bstar.md`（白石茉莉奈/天使萌/未步奈奈/渚光希）
- `厂商/KMP.md` —— KMP 实为厂商（ケイ・エム・プロデュース），故建在 `厂商/` 下而不入经纪公司
- `经纪公司/HANAYA PROJECT.md`、`经纪公司/カプセルエージェンシー.md`、`经纪公司/アトラクティブ.md`、`经纪公司/JETSTREAM.md`、`经纪公司/AINA Group.md`（アイナ）
- `经纪公司/ハーベスターズ.md` —— 核实 ALLURE 为ハーベスターズ旧社名（有限会社アリュール，フィットワン分离开独立、2018 年更名，日文维基成员表含 みづなれい/Hitomi），故按「改名后用现名」口径建ハーベスターズ页；Hitomi、水菜丽 的 tag 已从 `ALLURE` 统一为 `ハーベスターズ`（三语）
- 同步：`经纪公司/index.md` 与 `厂商/index.md` 补行与简介；`协会/日本プロダクション協会.md` 正会员表与退会名单对已有条目页加链接；`Bambi Promotion.md` 成员表修正（天使萌→现属 Bstar、楪可怜→现属 T-POWERS、北川ゆず→现属 ACT、あきばもも→现属 ARCHE，白上咲花补入现属）

以上均三语同步，`check_i18n.py` 与三语 strict 构建通过（356 files/language）。

## 5. 其他

- 系列目录文件名含特殊字符（`○○`、`☆`、`♡`、`！`、`。`），建链时注意转义
- `Mine_S.md` 因文件名避让单引号使用下划线，条目名统一为 `Mine's`
- 别名页 `は/は/桥本有菜.md` 与主条目 `あ/あ/新有菜.md` 互链用了 `../../`（二级目录层级），改这两个文件时注意

## 6. 英文版正文待重译（新增）

英文版是中文 → 英文的机器翻译初稿，未经修订。本次只修了数据与受保护区：

- `_meta/list.{yaml,md}`、`排名/*.yaml` 已按中文源重建；
- 12 个作品页的日文原文与中文对照两节已回填；
- H1、index 链接显示名、正文机翻（含 `** Broker**` 一类被误译的字段名）**未动**，例：`佐佐木明希` 正文原写 `Zhengxi (since 2025), former Zhifu (2015-2019)`。

重译需单独立项，规则见 `TRANSLATION.md`。
