# 待办事项

内容审查（2026-08-18）遗留的未改动项，供维护者决策。

## 1. 事务所（经纪公司）tag 命名整体策略

tag 目前英日混用：

- 英文：`T-POWERS`(40)、`Mine's`(7)、`8MAN`(5)、`LINX`(5)、`NAX`(4)、`LIGHT`(6)、`C-more`(5)、`ARROWS`(2)、`Diaz Group`(2)、`CRUSE GROUP`(1)、`ACT`(5)、`LIFE PROMOTION`(3)、`Bambi Promotion`(5)、`B-dash`(3)
- 日文：`フォーティーフォーマネジメント`、`プライムエージェンシー`、`オールプロモーション`、`カプセルエージェンシー`、`リスタープロ`、`名東`、`ジュンプロ`、`ジールグループ`、`アトラクティブ` 等

已按「有经纪公司条目的用条目名、其余用官方名」原则统一了 Bambi/B-dash/Mine's；如需整体日文化或整体英文化，需另行处理。

## 2. 女优基本信息格式不统一

- 多数页面用 `- **姓名**: ...` 列表式
- 少数页面用表格式（如 `鹰宮ゆい.md` 的 `| 项目 | 内容 |`）
- 男の娘等条目用「艺名」字段（如 `おりん.md`）

建议统一为一种格式（可保留「艺名」字段用于男の娘条目）。

## 3. 正文与 tag 拼写不一致

- `吉根ゆりあ.md`：正文写「ビーダッシュプロモーション」，tag 已统一为 `B-dash`
- `あきばもも.md`：tag 用 `ARCHE-Production`，正文写「アルシェ」
- 类似情况：`美園和花.md` 正文「B-dash」、`あずみ恋.md` 正文「B-Dash Promotion」（大小写）

## 4. 经纪公司条目缺失

经纪公司 README 提到以下公司但无对应条目页：

- **Prime Agency**（プライムエージェンシー）— 永井マリア、仁科百华、樱木凛 的 tag 在用
- **Allpro**（オールプロ）— 石川澪 等
- **LIGHT** — README 正文有「### LIGHT」段落但无 `LIGHT.md` 条目页
- **Bstar**（4 页 tag 在用）、**KMP**、**ALLURE**、**HANAYA PROJECT** 等

## 5. 其他

- 系列目录文件名含特殊字符（`○○`、`☆`、`♡`、`！`、`。`），建链时注意转义
- `Mine_S.md` 因文件名避让单引号使用下划线，条目名统一为 `Mine's`
