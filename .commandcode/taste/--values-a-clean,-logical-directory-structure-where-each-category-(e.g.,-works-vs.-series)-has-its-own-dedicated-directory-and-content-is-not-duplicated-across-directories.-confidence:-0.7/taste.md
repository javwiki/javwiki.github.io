# - Values a clean, logical directory structure where each category (e.g., works vs. series) has its own dedicated directory and content is not duplicated across directories. Confidence: 0.7
- Values a clean, logical directory structure where each category (e.g., works vs. series) has its own dedicated directory and content is not duplicated across directories. Confidence: 0.7
- Prefers individual work pages to include a dedicated 剧情简介 (plot summary) section with a narrative description of the work's content/story. Confidence: 0.6
- Prefers to completely wipe and start from scratch when existing content is fundamentally incorrect, rather than patching or incrementally fixing it. Confidence: 0.8
- When correcting work metadata that was obtained incorrectly from web search, prefers to provide a specific reference URL (e.g., from an aggregator site like 18mh.net) as the authoritative source rather than verbally listing corrections. Confidence: 0.3
- Communicates requests by providing only the bare JAV code (e.g., "AVOP-404") with zero additional instructions or context, expecting the assistant to autonomously handle the full workflow. Confidence: 0.8
- Expects the assistant to autonomously execute the complete workflow for each JAV code: web research → create individual work markdown file with frontmatter fields (type, title, number, studio, release_date) and sections (基本信息 table, 剧情简介, 作品特征) → update the README index. Confidence: 0.8
- When the user persistently flags that visually-observed data (e.g., kana characters, rendered text) looks incorrect, the assistant should consider whether the issue might be a terminal/environment font rendering problem before concluding the underlying data is wrong — verify display conditions before rewriting data. Confidence: 0.5
- Communicates git/version-control operations with extremely terse Chinese commands (e.g., "提交推送" for commit + push), expecting the assistant to autonomously handle the full git workflow: check status, stage only project files (excluding taste files), write a structured multi-line commit message, commit, and push to remote. Confidence: 0.7
- Expects the assistant to follow a two-phase workflow when dealing with project errors: first perform a comprehensive scan/diagnosis of all issues, then autonomously execute every identified fix without further guidance — a single terse command (e.g., "修复") signals "fix everything found in the scan". Confidence: 0.6
- When investigating missing/referenced content, prefers to use git history to trace file provenance (e.g., `git log --all -p -S` to find when a reference was added and whether the file ever existed), rather than accepting surface-level absence as conclusive. Confidence: 0.5
- Responds to assistant proposals for the next action step with a single-word affirmative (e.g., "需要" meaning "yes/do it") to green-light content creation or research, delegating decision-making to the assistant without additional instructions or scope changes. Confidence: 0.6
