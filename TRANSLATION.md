# 中日英文内容 i18n 迁移说明

本文记录 2026-09-20 将本站由单一中文内容树迁移为中文、日文和英文三语内容站点时使用的目录结构、翻译方式、结构契约、实测规模、验证结果和后续维护约定。

中文（`docs/zh/`）是唯一源内容。英文为机器翻译初稿；日文初版机翻不可用，已依据中文源重建，但同样不代表逐篇重新调查、重新编写或人工校订。任何一版都未替代事实核验。

## 发布结构

| 语言 | 内容目录 | 构建配置 | 发布路径 |
|---|---|---|---|
| 中文 | `docs/zh/` | `zensical.toml` | `/` |
| 日文 | `docs/ja/` | `zensical.ja.toml` | `/ja/` |
| 英文 | `docs/en/` | `zensical.en.toml` | `/en/` |

中文版继续发布在站点根路径，以保持迁移前的公开 URL 不变。各版本通过页眉语言选择器切换，编辑链接分别指向对应的语言目录。三个构建共用 `scripts/build_site.sh`，CI 在推送到 `main` 后执行同一脚本并部署到 GitHub Pages。

## 内容树结构

三个语言目录使用**完全相同的相对路径**，例如：

```text
docs/zh/法律/民法.md
docs/ja/法律/民法.md
docs/en/法律/民法.md
```

文件名和目录名一律不翻译。这使三棵树可以直接逐文件比对，也避免语言切换时维护额外的路径映射。代价是中文目录名（如 `../女优/index.md`）会出现在日文和英文页面的链接里。

每个语言目录共 357 个文件，按主题分布如下：

| 目录 | 文件数 | 说明 |
|---|---:|---|
| `あ` `か` `さ` `た` `な` `は` `ま` `や` `ら` `わ` | 228 | 女优条目，按五十音两级索引 |
| `系列` | 27 | 系列作品条目 |
| `作品` | 14 | 单个作品条目（按番号） |
| `经纪公司` | 23 | 经纪公司条目 |
| `法律` | 11 | 相关法令条目 |
| `组合` | 9 | 偶像组合条目 |
| `厂商` | 8 | 片商条目 |
| `活动` | 7 | 展会与活动条目 |
| `协会` | 5 | 行业协会与自律组织 |
| `_meta` | 5 | 维护用资料（见下） |
| `奖项` | 4 | 行业奖项条目 |
| `男优` | 4 | 男演员条目 |
| `导演` | 3 | 导演条目 |
| `排名` | 3 | FANZA 月榜数据与说明 |
| `专题` | 2 | 产业观察长文 |
| `女优` `术语` `番号` `index.md` | 各 1 | 分类说明与站点首页 |

### 五十音索引

女优条目按「行 / 段」两级存放：`docs/{行}/{段}/{艺名}.md`。行分 あ・か・さ・た・な・は・ま・や・ら・わ 共 10 行，段为各行内的 あ・い・う・え・お 等。条目归入哪个段由**艺名读音的首个假名**决定，浊音与半浊音并入所属行（が行归 か行，ば・ぱ 行归 は行）。

- 行索引 `docs/{行}/index.md` 只链接各段。
- 段索引 `docs/{行}/{段}/index.md` 列出该段全部条目。
- 行、段目录名本身就是假名，条目文件名则是艺名的中文写法。

排序与定位规则的完整说明见 `docs/{lang}/_meta/五十音排序规则.md`。

**相对链接的层级**：`{行}/{段}/` 下的页面处在**二级目录**，`../` 只会上退到 `{行}/`，要跨到根目录下的其它分类必须写 `../../`。写错层级的链接不会 404（构建出的目录 URL 结构不同），只会静默解析到错误目标，因此由 `check_i18n.py` 逐条解析校验：

```markdown
docs/zh/は/は/桥本有菜.md   →  [新有菜](../../あ/あ/新有菜.md)   # 正确
                              [新有菜](../あ/あ/新有菜.md)       # 错：解析成 は/あ/あ/
docs/zh/は/は/米倉穂香.md   →  [KBI-001](../../作品/KBI-001.md)  # 同理
```

一级目录页面（如 `经纪公司/index.md`）仍用一个 `../`。

### `_meta/`

| 文件 | 用途 |
|---|---|
| `list.yaml` | 结构化的女优列表：`title`、`items[].{name,row,col,completeness}`，与目录结构一一对应 |
| `list.md` | 与 `list.yaml` 同步的纯文本列表，便于人工比对 |
| `五十音排序规则.md` | 行段定义、汉字/片假名/混合名的定位规则、`list.yaml` 结构与一致性约束 |
| `source.md` | 外部资料来源、可靠性分级与引用格式 |
| `index.md` | 维护资料的入口索引 |

`排名/actress-ranking-YYYYMM.yaml` 为抓取所得的数据文件（采集脚本见 `scrapers/fanza/`），字段名使用英文，属机器可读数据而非正文。

页面导航由 Zensical 根据目录结构生成，并启用 `navigation.prune` 只完整渲染当前分支，不手工维护 `SUMMARY.md`、`book/` 或静态 `_tags/` 页面；标签通过页面 front matter 的 `tags` 与 `zensical.toml` 的原生 Tags 插件处理。`overrides/` 中的模板把语言 alternate 和语言选择器改为逐页对应路径。

## 迁移时规模（2026-09-20 基线）

| 指标 | 中文 | 英文 | 日文 |
|---|---:|---:|---:|
| 文件数 | 348 | 348 | 348 |
| UTF-8 大小 | 952,034 bytes | 1,091,535 bytes | 1,088,585 bytes |
| 字符数 | 562,271 | 1,067,095 | 604,268 |
| 汉字数量 | 141,991 | 4,061 | 107,192 |
| 假名数量 | 32,734 | 4,943 | 113,306 |
| 英文字词数量 | 38,655 | 159,462 | 38,536 |

统计涵盖 Markdown、YAML、URL 与未翻译的 front matter，并非纯正文规模，只用于观察三棵树的量级与构成。日文列为重建后的实测值；重建前该列为 1,599,469 bytes / 877,792 字符，其中包含大量重复退化文本，字符数虚高。

## 英文：双语句机器翻译

| 项目 | 本次使用值 |
|---|---|
| 翻译引擎 | Argos Translate 1.11.0 |
| 中文到英文模型 | OPUS-MT `translate-zh_en` 1.9 |
| 推理后端 | CTranslate2 4.8.2 |
| 设备与精度 | CPU、`int8` |
| 搜索参数 | `beam_size=1`、单候选 |
| 批处理参数 | `batch_size=32` |
| 并行方式 | 4 个文件分片、每个 worker 单线程推理 |

在本机实际约两核可用 CPU 的条件下，中文到英文翻译约 35 分钟，包含模型加载、批量推理、Markdown 重组与写盘，不应视为通用模型性能基准。英文版未做二次修订，`docs/en/` 的质量限制见下文。

## 日文：依据中文源重建

日文初版由「中文 → 英文 → 日文」二级机器翻译生成（OPUS-MT `translate-en_ja` 1.1，约 37 分钟）。该路径把英文初稿的误差再放大一次，产出不可用：

- **专有名词被意译或音译**：`蒼井そら` → `葵くん`，`相沢みなみ` → `アゼンアム`，`日文名` 字段值 → `彩公社事務所`；
- **中文源中保留的官方日文原文被英文回译摧毁**：`IPX-177` 的日文原文变成 `ヒエナ、ハイエナ、ハイエナ…`，作品名变成「地域は「甘み」と「甘み」の長い歴史を国に持ちます。」；
- **结构被当作正文翻译**：`_meta/list.yaml` 的 `items` → `アイテム`，排名 YAML 的 `rankings`/`rank` → `ランキング`/`ランク`，人名 → `申し訳ございません。`；
- **重复退化**：`全て正しいです。 アパート`、`年 年 年 年`，以及 `とりあえず` 之类的插入语。

因此日文版改为**依据中文源逐文件重新改写**，不再使用「中文 → 英文 → 日文」产物——中文源里本就保留着官方日文专名（`強制中出し 射精執行官`、`スカパー!アダルト放送大賞`、`AV出演被害防止・救済法`、`恵比寿★マスカッツ` 等），从中文源出发才能拿回这些写法。重写按目录切成 28 个分片并行执行，每个文件都先取 `docs/zh/` 对应页面的事实，再写成日语，不新增、不删减任何事实、数字、日期或链接。

### 演员名对照

演员名在三棵树里指向同一文件名，但显示写法不同。日文标准名来自两个固定来源：

1. 各条目的 `日文名` 字段（中文源自身持有）；
2. `scripts/ja_name_map.json`——迁移前批量抓取时使用的艺名映射表。

两者合成一张固定的「中文文件名 → 日文标准名」对照表（180 条），日文页面的 H1、`名前` 字段、链接显示文字、正文与表格统一使用该写法。例如 `相泽南`→`相沢みなみ`、`苍井空`→`蒼井そら`、`三上悠亚`→`三上悠亜`、`朝美穗香`→`みひろ`。表格中带空格的写法（如 `篠田 ゆう`、`澁谷 果歩`）沿用中文源 `日文名` 字段的原样。

### 确定性重建的部分

以下文件不适合逐篇改写，直接由中文源以脚本重建：

- `docs/ja/{行}/{段}/index.md` 等 47 个五十音索引页：条目链接的**显示名**换成日文标准名，**链接目标**保持中文源原样；表头改为 `## 記事一覧`、`## 段インデックス`。
- `docs/ja/_meta/list.{md,yaml}`：条目名换为日文标准名，`title` 改为日文，其余键名与结构不变。
- `docs/ja/排名/actress-ranking-*.yaml`：与中文源逐字节一致——这是数据文件，键名本就是英文，人名使用 FANZA 官方写法。
- front matter 的 `title:`：决定页面标题与导航显示名，改为日文并与各自 H1 一致（6 个页面涉及奖项、活动、专题）。

## 结构契约

日文重建与英文机翻都遵守同一套结构契约，可概括为「**结构冻结，正文翻译**」。

保持与中文源一致：

- YAML front matter（唯一例外见下）；
- 链接目标、裸 URL、图片 `src`、文件路径；
- 标题层级序列（数量、层级、顺序），标题文字翻译；
- 表格的行列数与分隔行，单元格文字翻译；
- 代码块数量与围栏语言标记，块内内容不翻译；
- HTML 标签及其属性（`alt` 除外，`alt` 改为该页主题在当语版本中的写法）。

会翻译的内容：标题、段落、列表正文、表格单元格、链接显示文字、`alt` 属性。

### 允许的差异

- **front matter 的 `title:`**：页面与导航的显示标题，按语言翻译。其余字段（`tags`、`thumbnail`、`type`、`number` 等）保持中文源原值，包括 `tags` 里仍是中文的词（`企画单体`、`跨性别`、`E杯`）。
- **作品页的引用小节（受保护区）**：`### 日文原文（missAV / FANZA）` 一节保存的是官方日文文案，三棵树逐字相同，因此在日文版中保留中文小节名不改；`### 中文翻译` 在日文版中改名为 `### 中国語訳`，正文仍为中文，作为对照保留；英文版对应 `### Original Japanese (missAV / FANZA)` 与 `### Chinese translation`。这两节**只能从 `docs/zh/` 原样拷贝，任何语言都不译**，包括其中的 `> 注` 前导说明行——它位于受保护区内，故中文版也保持中文，英文版同样保留中文（英文版的完整重译另行处理）。校验按节比对，忽略小节结尾多余的空行。
- **文件名、目录名与链接目标**：不翻译，因此日文、英文页面中出现中文路径与中文链接目标是预期行为。
- **中文源中只有中文译名的专有名词**：若中文源未给出官方日文写法（部分作品名、电影名、活动名），日文版按中文原文保留或按新字体转写，不臆造标题。

- **专名以字段为准，正文可保留官方原文**：当 front matter 的 `tags` 与正文写法冲突时，以结构化字段（`tags`、`经纪公司`/`所属事務所`）为裁定依据。若两者写法不同而指同一实体，正文首次出现改为「`条目名（官方原文）`」并列的形式，不改文件名。

### 事务所 tag 命名规范

2026-09 内容修复后，三语 `tags` 逐字节一致，并遵循以下口径（**刻意不做整体日化或英化**——那会破坏「官方写法」与「条目名」的绑定）：

1. **同一家公司只用一个 tag。** 不留 `前X` 与 `X` 双挂，也不留写法变体（如 `オールプロモーション` / `Allpro` 并存）。
2. **有经纪公司条目页的，tag 用条目名**（拉丁字母写法）：`Allpro`（= オールプロモーション）、`Prime Agency`（= プライムエージェンシー）、`8MAN`、`Diaz Group`、`ロータスグループ`、`Madonna`、`Prestige` 等。
3. **无条目页的，tag 用公司官方原文**，日文原文照抄（`カプセルエージェンシー`、`リスタープロ`、`名東`、`ジュンプロ`、`ジールグループ`、`アトラクティブ`）。
4. **「曾所属」写进正文的 `经纪公司`/`所属事務所` 字段**，不体现在 tag 里：写 `リスタープロ（2025年至今）、Allpro（オールプロモーション，前，2015-2019）`，tag 只保留 `Allpro` 与 `リスタープロ` 两项。
5. **改名后用现名**：`SO MODEL AGENT`（旧称 8MAN）统一用 `8MAN`；`オールプロ` / `All Promotion` 统一用 `Allpro`。
6. **条目页与 tag 必须互相印证**：`经纪公司/{公司}.md` 列出的成员，其 `tags` 中应含该 tag；反之正文写明现属该公司的，补上 tag（如 `北川绘里香` 补 `Allpro`）。

改 tag 后须同步三语，并检查正文对应字段是否用同一写法（见上一条「专名以字段为准」）。

## 验证

`scripts/check_i18n.py` 是内容校验的入口，依赖由根目录 `pyproject.toml` 与 `uv.lock` 固定。执行四类检查：

1. **文件集合**：三个语言目录的文件相对路径必须完全一致，多一个或少一个都报错（覆盖全部 357 × 3 个文件）。
2. **结构一致**：逐页比对 `docs/ja/`、`docs/en/` 与 `docs/zh/` 的 Markdown 页面——front matter（除 `title:`）、标题层级序列、按文档顺序排列的链接/自动链接/图片目标、表格形状（每行竖线数）、代码块数量与开启围栏的语言标记。共 708 页。
3. **链接与索引完整性**（按语言独立检查，不与中文源比对）：
   - 链接目的地不得含裸空格——正则若截断成 `Bambi Promotion.md` → `Bambi`，比对两侧反而一致，属于静默盲区；
   - 所有相对链接必须能解析到文件或含 `index.md` 的目录，二级目录页的 `../../` 层级由此兜住；
   - `{行}/{段}/index.md` 必须列出该段全部条目；
   - Markdown 中不得出现脚本、iframe、object、embed、form、事件属性或 `javascript:` 等主动 HTML（代码围栏示例除外）。
4. **数据完整性**：
   - `_meta/list.yaml`、`排名/*.yaml` 必须能解析为 YAML；
   - `list.yaml` 必须有 `title` 与 `items`，每条有非空的 `name`/`row`/`col`/`completeness`，无重名，且三语条数一致；
   - `list.md` 的名称及顺序必须与本语 `list.yaml` 一致；中文源 `row`/`col`/`name` 必须对应真实演员页面，目标语言可翻译显示名，但 `row`/`col`/`completeness` 不得漂移；
   - 中文源 `排名/*.yaml` 必须通过 schema 检查：顶层字段齐全、`count` 与行数相等、排名为连续 `1..N`、演员 ID 不重复、条目必填字段类型正确；
   - `排名/*.yaml` 必须与 `docs/zh/排名/` 逐字节一致；
   - `作品/` 页及 `活动/JAE.md` 的受保护区（日文原文、中文翻译两节）必须与中文源逐字节一致（忽略小节尾部空行数）。

```bash
uv run --locked --no-dev python scripts/check_i18n.py
```

实测输出：

```text
i18n trees match (357 files per language, 708 pages structurally checked)
```

`./scripts/build_site.sh` 会先跑上述检查，再依次以 Zensical 0.0.62 的 strict 模式构建三个语言版本，最后用 `check_site.py` 扫描全部生成页面的内部文件链接、锚点、逐页语言链接，并执行 HTML 160 KiB / 导航 320 链接的体积预算：

```bash
./scripts/build_site.sh
```

```text
i18n trees match (357 files per language, 708 pages structurally checked)
Build started
No issues found
Build finished in 3.18s
（日文 3.23s，英文 3.81s；均为已预热依赖的本地实测）
site links OK (1065 pages, 226638 link elements)
```

CI（`.github/workflows/zensical.yml`）在推送到 `main`、面向 `main` 的 `pull_request` 以及手动触发时运行测试、依赖审计和同一构建脚本。只有 `main` 的非 PR 事件才上传并部署 Pages；手动选择其它分支仍可验证构建，但不能发布。workflow 的权限收敛到 job 级（默认 `contents: read`，deploy 才授予 `pages: write` + `id-token: write`），全部 actions 固定到 commit SHA，runner、uv 与 Python 版本也固定。

除仓库校验器外，以下辅助检查用于验证迁移结果：

1. 扫描旧机翻残留词与重复退化模式（`とりあえず`、`全て正しいです`、`アパート`、`年 年 年`、`ヒエナ` 等）：日文正文中为 0。仍会命中的中文串只落在受保护区域——链接目标（如 `zh.wikipedia.org/wiki/AIKA_(AV女优)`）、front matter 的 `tags` 值、示例代码块内的路径、`### 中国語訳` 的中文对照正文。
2. 扫描日文页面的中文残留词（`信息`、`视频`、`片商`、`女优`、`导演`、`出道` 等）：正文中为 0。
3. 用 Playwright 渲染 `site/ja/` 抽查女优页、作品页、法令页、奖项页、厂商页与索引页，确认页面标题、章节名与正文均为日语。
4. **自动回归测试**：`tests/test_check_i18n.py` 固化链接顺序、围栏语言、列表映射和排名 schema 的正反例；`tests/test_workflow.py` 锁定生产部署分支、runner 与锁文件构建约束。CI 每次执行 `pytest`、Ruff 和 `pip-audit`。

以上检查只能确认结构一致、数据完整与站点可构建，都不能证明译文准确。

## 质量限制

英文内容（机器翻译初稿，未经修订）可能存在：

- 人名、作品名、厂商名、机构名和法律名被错误翻译；
- 行业术语、五十音说明或中文音译被错误解释；
- 性别代词、单复数、时态、冠词与语序不自然；
- 中文源中的日文名称被当作待译文本处理；
- 链接文字与目标页面的正式名称不一致；
- YAML front matter 保持中文源值，与页面正文语言不同。

日文内容经过重建后已消除上述机翻错误的主要来源，但仍存在：

- 日语为改写产物，未逐篇人工校订，语序与用词仍可能生硬；
- 中文源自身含混或自相矛盾的表述（日期冲突、表格列数不一致等）按原样保留，未做事实核验；
- 中文源只有中文译名的专有名词按中文原文保留，可能不等于官方标题；
- front matter 的 `tags` 值与文件内出现的链接目标是中文，与正文语言不同。

人物资料、日期、数字、作品信息与来源均以中文源条目为基础。英文内容上线后应视为待人工校订版本；日文内容视为已可读、仍待润色的版本。

## 后续维护流程

新增、删除或修改页面时：

1. 先更新 `docs/zh/` 中的中文源内容。
2. 同步更新 `docs/ja/` 和 `docs/en/` 下相同相对路径的页面；若新增或删除文件，三个版本同时增删。
3. 新增女优条目时，按 `docs/zh/_meta/五十音排序规则.md` 定位 `{行}/{段}/`，并把链接插入 `docs/{lang}/{行}/{段}/index.md` 与该语言的 `_meta/list.md`、`_meta/list.yaml`（`list.yaml` 三语条数必须一致，否则校验失败）。
4. 新增或删除文件后，确认相应 `index.md` 能到达该页；二级目录页跨分类链接写 `../../`。
5. 改 tag 或经纪公司写法时，按「事务所 tag 命名规范」三语同步，并同步 `经纪公司/{公司}.md` 的成员表。
6. 运行完整校验：

   ```bash
   ./scripts/build_site.sh
   ```

   若只想快速检查结构与数据，运行：

   ```bash
   uv run --locked --no-dev python scripts/check_i18n.py
   ```

### 人工校订清单

- 保留人物艺名、厂商和品牌的官方写法；
- 对照来源恢复作品、论文、奖项和法律的正式名称；
- 日文页面的演员名以中文源条目的 `日文名` 字段为准，并与既有页面写法核对一致；
- 检查日文正文中的中文残留词与旧机翻残留；
- 检查英文代词、时态、冠词和单复数；
- 不新增中文源与公开来源未支持的事实，不改动数字、日期与链接目标；
- 不修改文件名与目录结构；
- 校订后再次运行 `./scripts/build_site.sh`。
