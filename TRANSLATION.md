# 中日英文内容 i18n 迁移说明

本文记录 2026-09-20 将本站由单一中文内容树迁移为中文、日文和英文三语内容站点时使用的目录结构、翻译模型、Markdown 处理方式、实测规模、验证结果和后续维护约定。

中文是源内容。英文为机器翻译初稿；日文经过重建（见下节），依据中文源改写，但同样不代表逐篇重新调查、重新编写或人工校订。

## 发布结构

| 语言 | 内容目录 | 构建配置 | 发布路径 |
|---|---|---|---|
| 中文 | `docs/zh/` | `zensical.toml` | `/` |
| 日文 | `docs/ja/` | `zensical.ja.toml` | `/ja/` |
| 英文 | `docs/en/` | `zensical.en.toml` | `/en/` |

中文版继续发布在站点根路径，以保持迁移前的公开 URL 不变。各版本通过页眉语言选择器切换，编辑链接分别指向对应的语言目录。

三个目录使用完全相同的相对路径。例如：

```text
docs/zh/法律/民法.md
docs/ja/法律/民法.md
docs/en/法律/民法.md
```

文件名和目录名不翻译。这样可以直接检查三个内容树是否一一对应，也能避免语言切换时维护额外的路径映射。

## 迁移规模

| 指标 | 中文 | 英文 | 日文 |
|---|---:|---:|---:|
| 文件数 | 345 | 345 | 345 |
| UTF-8 大小 | 952,034 bytes | 1,091,535 bytes | 1,088,585 bytes |
| 字符数 | 562,271 | 1,067,095 | 604,268 |
| 汉字数量 | 141,991 | 4,061 | 107,192 |
| 假名数量 | 32,734 | 4,943 | 113,306 |
| 英文字词数量 | 38,655 | 159,462 | 38,536 |

上述统计包含 Markdown、YAML、URL 和未翻译的 front matter，并非纯正文规模。日文列为重建后的实测值；重建前该列为 1,599,469 bytes / 877,792 字符，其中包含大量重复退化文本。

## 翻译工具与模型

### 英文：双语句机器翻译

| 项目 | 本次使用值 |
|---|---|
| 翻译引擎 | Argos Translate 1.11.0 |
| 中文到英文模型 | OPUS-MT `translate-zh_en` 1.9 |
| 推理后端 | CTranslate2 4.8.2 |
| 设备与精度 | CPU、`int8` |
| 搜索参数 | `beam_size=1`、单候选 |
| 批处理参数 | `batch_size=32` |
| 并行方式 | 4 个文件分片、每个 worker 单线程推理 |

在本机实际约两核可用 CPU 的条件下，中文到英文翻译约 35 分钟。时间包含模型加载、批量推理、Markdown 重组和写盘，不应视为通用模型性能基准。

### 日文：基于中文源的重建

日文初版由「中文 → 英文 → 日文」二级机器翻译生成（OPUS-MT `translate-en_ja` 1.1，约 37 分钟）。该路径把英文初稿的误差再放大一次，产出不可用：

- 专有名词被意译或音译（`蒼井そら` → `葵くん`，`相沢みなみ` → `アゼンアム`，`S1 NO.1 STYLE` → `エスワン` 等）；
- 中文源中保留的官方日文原文被英文回译摧毁（`IPX-177` 的日文原文变成 `ヒエナ、ハイエナ…`）；
- 标题、表格表头、`_meta/list.yaml` 与排名 YAML 的键名被翻译，破坏数据字段；
- 出现重复退化文本（`全て正しいです。 アパート`、`年 年 年 年`）与 `とりあえず` 之类的插入语。

因此日文版改为**依据中文源逐文件重新改写**，不再使用「中文 → 英文 → 日文」产物。重建按目录分片并行执行，每个文件都先从 `docs/zh/` 对应页面取得事实，再写成日语。演员名的对应关系固定记录在改名前使用的 `scripts/ja_name_map.json`，并叠加各条目的 `日文名` 字段，形成固定的「中文文件名 → 日文标准名」对照表。

## Markdown 与结构处理

迁移使用的一次性脚本按行解析 Markdown 和 YAML，再把可翻译文本批量送入模型。脚本未提交到仓库。

以下内容保持原样：

- Markdown 代码块与行内代码；
- 链接目标、裸 URL 和文件路径；
- Markdown 标题、列表、引用和表格的结构标记；
- Markdown 文件的 YAML front matter；
- 不需要翻译的空行和表格分隔行。

以下内容会翻译：

- 标题、段落和列表正文；
- 表格单元格；
- Markdown 链接的显示文字；
- 独立 YAML 文件中的可翻译值。

日文重建沿用同一套结构约束，并额外要求：front matter 与中文源逐字节一致（唯一例外是 `title:`，它决定页面标题与导航显示名，需译为日文并与 H1 一致）、链接目标与 `src` 属性集合一致、标题层级序列一致、表格行列数一致、代码块内容逐字节一致。数据文件 `docs/ja/排名/*.yaml` 直接与中文源保持一致，不翻译键名与取值。`tags:` 与 `thumbnail:` 等字段保持中文源原值。

### 链接保护

最初尝试将链接目标替换为文本占位符后整体翻译。模型会偶尔删除、改写或移动占位符，甚至把它移入链接标签，因此仅靠翻译后的正则替换不能可靠恢复 Markdown。

最终流程在送入模型前将链接拆成显示文字和目标两部分：只翻译显示文字，再由脚本明确重建 `[译文](原目标)`。URL、行内代码等保护片段采用相同的“模型外重组”方式。正文翻译按分片写入临时缓存，使格式修复不需要重复执行整批模型推理。

## 验证结果

迁移完成后执行了以下检查：

1. `python3 scripts/check_i18n.py`：三个语言目录均为 345 个文件，相对路径完全一致。
2. 逐文件提取 Markdown 链接目标和裸 URL：中文到英文为 0 个差异，英文到日文为 0 个差异。
3. 扫描翻译占位符：英文和日文内容中均无迁移标记残留。
4. `./scripts/build_site.sh`：三个语言配置均以 Zensical 0.0.62 的 strict 模式构建成功，并报告 `No issues found`。
5. 构建产物包含 `site/index.html`、`site/ja/index.html` 和 `site/en/index.html`。

日文重建后又执行了以下检查：

1. 逐文件比对 `docs/zh/` 与 `docs/ja/`：front matter 逐字节一致、标题层级序列一致、链接目标与图片 `src` 集合一致、表格行列数一致、代码块数量一致，345 个文件全部通过。
2. 扫描旧机翻残留词与重复退化模式（`とりあえず`、`全て正しいです`、`アパート`、`年 年 年` 等）：正文中为 0。仍会命中的中文串只出现在受保护区域——链接目标（如 `zh.wikipedia.org/wiki/AIKA_(AV女优)`）、front matter 的 `tags` 值、代码块内的路径示例、以及 `### 中国語訳` 的中文对照正文。
3. `docs/ja/index.md` 等在导航中显示的页面标题改为日文，与各自的 H1 一致。
4. 用 Playwright 渲染 `site/ja/` 抽查女优页、作品页、法令页、奖项页与索引页，确认标题、章节名与正文均已是日语。

路径一致性检查只能确认文件集合相同，strict 构建只能确认站点可构建；两者都不能证明译文准确。

## 质量限制

英文内容可能存在以下问题：

- 人名、作品名、厂商名、机构名和法律名被错误翻译；
- 行业术语、五十音说明或中文音译被错误解释；
- 性别代词、单复数、时态、敬体和语序不自然；
- 原文中的日文名称在中文到英文阶段被意外翻译；
- 链接文字与目标页面的正式名称不一致；
- YAML front matter 保持中文源值，可能与页面正文语言不同。

日文内容经过重建后已消除上述机翻错误的主要来源，但仍存在以下限制：

- 日语表达为改写产物，未逐篇人工校订，敬体、语序与用词仍可能有生硬之处；
- 中文源本身含混或自相矛盾的表述（日期冲突、表格列数不一致等）按原样保留，未做事实核验；
- 中文源中只有中文译名、没有官方日文写法的作品名、电影名与专有名词，按中文原文保留或按新字体转写，可能不等于官方标题；
- YAML front matter 的 `tags` 值仍为中文源值（如 `企画单体`、`跨性别`、`E杯`），与页面正文语言不同；
- 文件名、目录名与链接目标不翻译，因此中文链接目标（如 `../女优/index.md`）会出现在日语页面中。

任何机翻与重建都没有替代事实核验。人物资料、日期、数字、作品信息和来源均以中文源条目为基础。英文内容上线后应视为待人工校订版本。

## 后续维护流程

新增、删除或修改页面时：

1. 先更新 `docs/zh/` 中的中文源内容。
2. 同步更新 `docs/ja/` 和 `docs/en/` 下相同相对路径的页面。
3. 新增或删除文件时，同步维护三个版本的索引和 `_meta` 文件。
4. 人工检查人名、专有名词、代词、日期、数字、链接和引用标题。
5. 确认三个版本的链接目标仍指向相同的相对页面。
6. 运行完整校验：

   ```bash
   ./scripts/build_site.sh
   ```

该命令会先执行三语文件树检查，再依次严格构建中文、日文和英文站点。

如只需检查文件是否一一对应，可运行：

```bash
python3 scripts/check_i18n.py
```

### 人工校订清单

- 保留人物艺名、厂商和品牌的官方写法；
- 对照来源恢复作品、论文、奖项和法律的正式名称；
- 日文页面的演员名以中文源条目的「日文名」字段为准，改名前先核对是否与既有页面写法冲突；
- 检查日文中的中文残留词（`信息`、`视频`、`片商`、`女优`、`导演`、`出道` 等）与旧机翻残留（`とりあえず`、`全て正しいです`）；
- 检查英文代词、时态、冠词和单复数；
- 不新增中文源内容和公开来源未支持的事实；
- 不修改链接目标、文件名和目录结构；
- 校订后再次运行完整三语构建。
